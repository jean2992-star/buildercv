<!-- Modelo 3 – Timeline Minimal -->
<div class="cv m3">
  <header class="head">
    <h1>{{nome}}</h1>
    {{#if titulo}}<div class="role">{{titulo}}</div>{{/if}}
    <div class="line">
      {{#if email}}<span>✉️ {{email}}</span>{{/if}}
      {{#if telefone}}<span>📞 {{telefone}}</span>{{/if}}
      {{#if local}}<span>📍 {{local}}</span>{{/if}}
      {{#if links}}<span>🔗 {{links}}</span>{{/if}}
    </div>
  </header>

  {{#if resumo}}
  <section>
    <h2>Resumo</h2>
    <p>{{resumo}}</p>
  </section>
  {{/if}}

  {{#if experiencias.length}}
  <section>
    <h2>Experiência</h2>
    <ul class="timeline">
      {{#each experiencias}}
      <li>
        <div class="dot"></div>
        <div class="content">
          <div class="top">
            <strong>{{cargo}}</strong> — {{empresa}}
            <small>({{inicio}}{{#if fim}} – {{fim}}{{/if}})</small>
          </div>
          <ul class="bullets">
            {{#each (splitLines descricao)}}{{#if this}}<li>{{this}}</li>{{/if}}{{/each}}
          </ul>
        </div>
      </li>
      {{/each}}
    </ul>
  </section>
  {{/if}}

  {{#if educacao.length}}
  <section>
    <h2>Educação</h2>
    {{#each educacao}}
      <p><strong>{{curso}}</strong> — {{instituicao}} <small>({{inicio}}–{{fim}})</small></p>
    {{/each}}
  </section>
  {{/if}}

  {{#if skills}}<section><h2>Habilidades</h2><p>{{skills}}</p></section>{{/if}}
  {{#if idiomas}}<section><h2>Idiomas</h2><p>{{idiomas}}</p></section>{{/if}}

  {{!-- Extras padronizados --}}
{{#if cursos}}
<section class="block">
  <h2>Cursos e Certificações</h2>
  <p class="multiline">{{cursos}}</p>
</section>
{{/if}}

{{#if softwares}}
<section class="block">
  <h2>Softwares</h2>
  <p class="multiline">{{softwares}}</p>
</section>
{{/if}}

{{#if atividades}}
<section class="block">
  <h2>Atividades</h2>
  <p class="multiline">{{atividades}}</p>
</section>
{{/if}}

{{#if infoExtra}}
<section class="block">
  <h2>Informações Complementares</h2>
  <p class="multiline">{{infoExtra}}</p>
</section>
{{/if}}

</div>
